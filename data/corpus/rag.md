# 检索增强生成（RAG）

检索增强生成（Retrieval-Augmented Generation，简称 RAG）是一种在生成之前先从外部知识库检索相关证据的范式。

RAG 的核心动机是降低大语言模型的幻觉：模型不再只依赖训练时记住的参数化知识，而是先检索再生成。

一个典型的 RAG 流水线包含摄入、切分、嵌入、向量检索、词法检索、融合、重排、生成与引用等阶段。

RAG 特别适合企业知识库问答、文档助手与合规检索等场景。
